import { MultipleChoiceOption } from './multiple-choice-option';

describe('MultipleChoiceOption', () => {
  it('should create an instance', () => {
    expect(new MultipleChoiceOption()).toBeTruthy();
  });
});
